<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<%- include('./partials/nav') %>
<body>
  <h1>Full Menu</h1>
  <ul>
    <% mENU.forEach((item, index)=>{ %>
      <a href="/<%= index %>">
        <li>
          <%= item.name %>: <%= item.price%>,<%= item.rating%>,<%= item.category%>,<%= item.details%>
        </li>
      </a>
    <% }) %>
</body>
</html>
